[pytest]
testpaths = tests
markers =
    unit: Unit tests — isolated module behavior
    integration: Integration tests — cross-module data flow
    stress: Stress tests — high volume, adversarial, spam resistance
    e2e: End-to-end tests — full runtime and API validation
addopts = -v --tb=short
